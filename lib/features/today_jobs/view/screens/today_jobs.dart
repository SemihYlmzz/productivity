import '../../../features.dart';

class Enterance extends ConsumerWidget {
  const Enterance({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return const CupertinoPageScaffold(
      /*todayJobsAppbar*/
      child: SafeArea(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
